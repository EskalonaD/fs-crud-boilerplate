export const config = {
    data_storage_path: `${__dirname}/data`,
    backup_storage_path: `${__dirname}/backup`,
    backup: false,
    auth: false,
}

// export const testFunc = () => console.log(__dirname)
